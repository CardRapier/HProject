import 'package:flutter/material.dart';

class CustomHabitForm extends StatelessWidget {
  const CustomHabitForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Custom Habit Form'),
      ),
    );
  }
}
